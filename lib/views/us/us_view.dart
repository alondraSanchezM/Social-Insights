import 'package:flutter/material.dart';

class UsView extends StatelessWidget {
  const UsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Us'),
    );
  }
}